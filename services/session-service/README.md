# Session Service

## Purpose

Own sessions, messages, thinking traces, defaults, and per-session overrides.

## Depends On

- no required internal services in V1

## Standalone Development

Should support an in-memory driver and a file/DB-backed driver so session behavior can be built before the final persistence choice is locked.
