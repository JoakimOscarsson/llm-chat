# Model Service

## Purpose

Own discovery and normalization of available upstream models.

## Depends On

- `ollama-adapter`

## Standalone Development

Should support mocked provider model lists and configurable cache TTL behavior.
